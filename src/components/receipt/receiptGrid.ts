// Shared column template for the receipt's DATE / ITEM / PAID BY / AMOUNT / * grid,
// so the column header, expense rows, and any nested row (e.g. split breakdown)
// stay pixel-aligned with each other.
export const RECEIPT_GRID_COLS =
  "grid-cols-[3.4rem_1fr_3.4rem_3.6rem_0.75rem] sm:grid-cols-[3.8rem_1fr_3.8rem_4rem_0.75rem]";
